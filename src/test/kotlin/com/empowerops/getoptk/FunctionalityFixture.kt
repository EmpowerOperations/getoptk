package com.empowerops.getoptk

import org.junit.Test

//this fixture is the more verbose version of UsageExamples.
// it should test smaller and more obscure cases than those in UsageExamples.kt
class FunctionalityFixture {

    @Test
    fun `when supplying converter for subtype should be used to produce value for supertype`(){
        TODO("this is to test that if I know how to convert things into a subtype, " +
                "eg a windows path, " +
                "when my declared type is supertype, " +
                "eg Path, " +
                "I should use that converter"
        )
    }


}